<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
      "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.team_pj_springboot.mappers.CompanyMapper">

	<!-- MAX -->
    <select id="findMaxId" resultType="Integer">
        SELECT MAX(CAST(company_id AS NUMBER)) AS id FROM COMPANY
    </select>
</mapper>